-- UPDATE is used to update the data on existing table.we can update one or multiple columns.
-- Where clause in UPDATE statement to update only single row. without where clause it will update all rows.

update students 
set name = 'Rupasri' 
where age = 21;
